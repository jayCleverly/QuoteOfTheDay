{
    "name": "quote-of-the-day",
    "description": "Gets a quote of the day from a public api and sends it out to an email list",
    "version": "1.0.0",

    "engines": {
        "node": ">=20.0.0 <23.0.0"
    },
    "main": "src/index.ts",

    "scripts": {
        "test": "jest",
        "coverage": "jest --coverage",
        "clean": "rm -rf dist",
        "mkdirs": "mkdir dist",
        "webpack": "webpack",
        "build": "npm install && npm run test && npm run clean && npm run mkdirs && npm run webpack"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.146",
        "@types/jest": "^29.5.14",
        "ts-jest": "^29.2.5",
        "webpack": "^5.95.0",
        "webpack-cli": "^5.1.4",
        "ts-loader": "^9.5.1"
    },
    "dependencies": {
        "axios": "^1.7.9",
        "@aws-sdk/client-ses": "^3.787.0"
    }
}
