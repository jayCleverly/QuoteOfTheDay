{
    "name": "joke-of-the-day",
    "description": "Gets a random joke and sends it out",
    "version": "0.0.1",

    "scripts": {
        "test": "jest",
        "coverage": "jest --coverage",
        "clean": "rimraf dist",
        "webpack": "webpack",
        "build": "npm install && npm run test && npm run clean && npm run webpack"
    },

    "dependencies": {
        "axios": "^1.7.9"
    },

    "devDependencies": {
        "@types/aws-lambda": "^8.10.146",
        "@types/jest": "^29.5.14",
        "jest": "^29.7.0",
        "ts-jest": "^29.2.5",
        "rimraf": "^3.0.0",
        "webpack": "^5.0.0",
        "webpack-cli": "^4.0.0",
        "webpack-node-externals": "^3.0.0",
        "ts-loader": "^9.0.0"
    }
}
